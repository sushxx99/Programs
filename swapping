#include <stdio.h>
void main()
{
int a, b,temp;
printf("Enter values for a and b:\n");
scanf("%d%d", &a,&b);
printf("Before swapping: a=%d, b=%d\n",a,b);
temp = a;
a = b;
b = temp;
printf("After swapping: a=%d, b=%d",
		a, b);
}
