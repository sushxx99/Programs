#include<stdio.h>
void main()
{
int a,b;
printf("Enter two numbers to find the largest no.:");
scanf("%d%d",&a,&b);
if(a>b)
printf("The greatest number is:%d",a);
else
printf("The greatest number is:%d",b);
}
